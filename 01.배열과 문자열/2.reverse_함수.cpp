#include <iostream>
#include <string>

using namespace std;

void reverse(char* str) {
	char c = str[0]
	while (c != NULL) {
		
	}
}

int main() {
	int a;
	char str[1000];

	getline(cin, str);

	reverse(str);
	
	cout << str << endl;

	return 0;
}